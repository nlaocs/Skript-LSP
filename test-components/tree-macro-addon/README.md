# Tree macro test addon

A deterministic WASM fixture for parser-wasm Tree macro integration tests. It exercises node deletion, one-to-many replacement, Section body preservation/replacement, recursive expansion, cycle detection, typed rejection, traps, invalid edits, and transactional StateStore writes.