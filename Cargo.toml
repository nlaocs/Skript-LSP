[package]
name = "skript-lsp"
version = "0.1.0"
edition = "2024"

[workspace]
members = ["skripthub", "ssg", "syntax-pattern-parser", "syntaxes", "utilities/*"]

[dependencies]
syntax-pattern-parser = { path = "syntax-pattern-parser" }
